spring:
  application:
    name: geuphalttaen

  profiles:
    include:
      - db
      - redis
      - jwt
      - opendata
      - ncp
      - kakao

  servlet:
    multipart:
      max-file-size: 50MB
      max-request-size: 52MB

  task:
    scheduling:
      pool:
        size: 2

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health, info
