spring:
  application:
    name: geuphalttaen

  profiles:
    include:
      - db
      - redis
      - jwt
      - opendata
      - ncp
      - kakao
      - r2

  servlet:
    multipart:
      max-file-size: 20MB
      max-request-size: 50MB

  task:
    scheduling:
      pool:
        size: 2

springdoc:
  swagger-ui:
    path: /swagger-ui.html
  api-docs:
    path: /v3/api-docs

management:
  endpoints:
    web:
      exposure:
        include: health, info
